# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
    config.vm.box = 'CatPlusPlus/Debian'
    config.vm.provider 'virtualbox' do |vb|
        vb.memory = '1024'
        vb.cpus = 2
    end
end
